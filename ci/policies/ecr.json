{
    "rules": [
        {
            "rulePriority": 1,
            "description": "Keeps only the last n images.",
            "selection": {
                "tagStatus": "any",
                "countType": "imageCountMoreThan",
                "countNumber": {{ECR_LIFECYCLE_COUNT}}
            },
            "action": {
                "type": "expire"
            }
        }
    ]
}
